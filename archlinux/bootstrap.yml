---

  - hosts: localhost
    connection: local

    roles:

      - role: bootstrap

  - hosts: localhost
    connection: local

    tasks:

      - name: start configure
        systemd:
          name: configure
          state: started
