---

  - hosts: localhost
    connection: local

    roles:

      - role: bootstrap

    post_tasks:

      - name: start configure
        command: systemctl start configure
        #systemd:
        #  name: configure
        #  state: started
