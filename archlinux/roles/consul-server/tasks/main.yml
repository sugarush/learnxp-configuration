---

  - name: template consul server configuration
    template:
      src: server.json
      dest: /etc/consul/agent/server.json

  - name: template consul bootstrap configuration
    template:
      src: bootstrap.json
      dest: /etc/consul/agent/bootstrap.json
