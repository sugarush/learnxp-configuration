{
  "service": {
    "id": "influxdb-relay",
    "name": "influxdb-relay",
    "tags": [
      "{{ lookup('env', 'ENVIRONMENT') }}"
    ],
    "address": "{{ lookup('env', 'HOST_IP') }}",
    "port": 8085,
    "checks": [
      {
        "args": [ "systemctl", "is-active", "influxdb-relay" ],
        "interval": "30s"
      },
      {
        "tcp": "{{ lookup('env', 'HOST_IP') }}:8085",
        "interval": "30s",
        "timeout": "1s"
      }
    ]
  }
}
