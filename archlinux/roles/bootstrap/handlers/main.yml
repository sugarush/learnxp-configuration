---

  #  These handlers exist here for the Docker role in the bootstrap module

  - name: reload consul@client
    systemd:
      name: consul@client
      state: reloaded
      daemon_reload: yes

  - name: reload consul@server
    systemd:
      name: consul@server
      state: reloaded
      daemon_reload: yes
