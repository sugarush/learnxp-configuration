---

  - name: copy consul client configuration
    copy:
      src: client.json
      dest: /etc/consul/agent/client.json

  - name: enable and start consul@client
    systemd:
      daemon_reload: yes
      name: consul@client
      state: started
      enabled: yes
