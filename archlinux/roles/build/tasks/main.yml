---

  - name: install pip
    command: easy_install-2.7 pip

  - name: install aws-cli
    command: pip install awscli

  - name: add build user
    user:
      name: build
      group: users
      groups: wheel,docker
