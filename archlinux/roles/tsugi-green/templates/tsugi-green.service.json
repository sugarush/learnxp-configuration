{
  "service": {
    "id": "tsugi-green",
    "name": "tsugi-green",
    "tags": [
      "{{ lookup('env', 'ENVIRONMENT') }}",
      "tsugi",
      "green"
    ],
    "address": "{{ lookup('env', 'HOST_IP') }}",
    "port": 80,
    "checks": [
      {
        "tcp": "{{ lookup('env', 'HOST_IP') }}:80",
        "interval": "30s",
        "timeout": "1s"
      }
    ]
  }
}
