{
  "service": {
    "id": "tsugi-green",
    "name": "tsugi-green",
    "tags": [
      "{{ lookup('env', 'ENVIRONMENT') }}",
      "tsugi",
      "green"
    ],
    "address": "{{ lookup('env', 'HOST_IP') }}",
    "port": 9000,
    "checks": [
      {
        "tcp": "{{ lookup('env', 'HOST_IP') }}:9000",
        "interval": "30s",
        "timeout": "1s"
      }
    ]
  }
}
