---

  - hosts: localhost
    connection: local

    roles:

      - role: bootstrap

  - hosts: localhost
    connection: local

    tasks:

      - name: wait for external connectivity
        wait_for:
          host: google.com
          port: 80

      - name: start configure
        systemd:
          name: configure
          state: started
